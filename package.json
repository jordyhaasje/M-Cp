{
  "name": "hazify-platform",
  "private": true,
  "version": "1.0.0",
  "description": "Hazify production monorepo for license service and remote MCP",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "sync:shared": "node scripts/sync-shared-packages.mjs",
    "verify:shared": "node scripts/verify-shared-packages.mjs",
    "check:git-sync": "node scripts/check-git-sync.mjs",
    "smoke:prod": "node scripts/smoke-prod.mjs",
    "build": "npm run sync:shared && npm run -ws --if-present build && npm run verify:shared",
    "test": "npm run verify:shared && npm run -ws --if-present test && npm run test:e2e",
    "test:e2e": "node tests/e2e/contract.test.mjs",
    "start:license": "npm run --workspace @hazify/license-service start",
    "start:mcp": "npm run --workspace @hazify/mcp-remote start:remote"
  },
  "engines": {
    "node": ">=18.18.0"
  }
}
